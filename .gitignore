# Secrets & credentials
.env

# Telethon session files (contain your auth token — never commit these)
*.session
*.session-journal

# Replay state (tracks which messages have been forwarded — local only)
replay_state.json

# Local config (copy config.py.example → config.py and fill in your values)
config.py

# Dev notes
/rmd.md

# Snapshots / local tooling
/.snapshots

# Python
/.venv
/__pycache__
**/__pycache__
*.pyc
*.pyo
*.pyd

# OS
.DS_Store
Thumbs.db
